import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    btnContaniner: {
        paddingVertical: 15,
        paddingHorizontal: 108,
        borderRadius: 10,
    },
    btnText: {
        fontSize: 18,
    },

})